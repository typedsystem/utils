from utils.mods.path.enriched import Path, File

class path_prop:
    absof  = Path.absof
    baseof = Path.baseof
    fileof = Path.fileof
    extof  = Path.extof
    mimeof = Path.mimeof
    encof  = Path.encof

class file_prop:
    linesof = File.linesof
